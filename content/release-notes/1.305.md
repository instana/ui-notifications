# Release 1.0.305

Release 1.0.305 is released on 22 September 2025.

<div class="note note"><span class="notetitle">Note:</span>

For the latest agent, tracer, and sensor updates, see the following release notes:

<ul>

<li><a href="../agent_release_notes/index.html">Agent releases</a></li>

<li><a href="../sensor_release_notes/index.html#recently-updated-sensors">Sensor releases</a></li>

<li><a href="../tracer_release_notes/index.html">Tracer releases</a></li>

</ul>

</div>

## Features

The following features are added in release 1.0.305:

### Synthetic monitoring

To streamline your synthetic monitoring experience, the following updates are now available on the Synthetic monitoring dashboard in the Instana UI:

- A contextual onboarding banner for synthetic monitoring is added on the Synthetic tests page. 
- The **Test execution** filter is now available on the following pages: **Synthetic tests**, **Summary**, and **Results**. The test execution types are also supported in [Synthetic Smart Alerts](../../synthetic_monitoring/smart_alerts.md) and [Synthetic Service Levels](../../service_level_objectives/slo_capabilities.md). 
- You can now create an on-demand test from the UI. For more information, see [On-demand synthetic tests](../../synthetic_monitoring/test_execution_types.md).

### Mobile app monitoring 

You can now view error messages and stacktraces generated for ANR beacons in the performance dashboard. For more information, see [collecting ANR details.](../../mobile_app_monitoring/performance.md#app-not-respondig-or-freezing-table).

## Improvements

The following improvements are provided in release 1.0.305:

### Application perspectives

Added a safeguard that limits 50 application perspectives per call. This limitation applies separately to "source applications" and "destination applications". For more information, see [Limitations of application perspectives](../../application_monitoring/application-perspectives.md#limitations-of-application-perspectives).

### Live tail of logs (public preview)

Instana UI is updated for easier log filtering. This enhancement includes improved search functionality and visual enhancements for a better user experience.

### Removed Customer Adoption page

The **Customer Adoption** page is removed intentionally to simplify the user experience.

## Fixes

The following issues are fixed in release 1.0.35:

### Handling of call HTTP parameters with no name

Previously, HTTP query strings that specified a parameter with no (empty) name were incorrectly decoded by using the value as the name. For example, a call with the query string `=val1&key=val2` resulted in HTTP parameters `=val1` (incorrect) and `key` (correct). Parameters with no name are now ignored. So now this call has only a `key` parameter. The full query string can still be observed in the call details.

### ELK integration

Fixed an issue that generated incorrect links to the configured ELK instance.

### Log Analytics filter suggestions

Fixed an issue that caused filter suggestions to stop working in **Analytics** > **Logs**.

### Synthetic monitoring

The security vulnerability `CVE-2025-27151` is fixed now.

## Rewards

Provide a review of Instana to receive a $25 Gift Card. To submit your review, go to [Submit review](https://ibm.biz/BdGY89).
